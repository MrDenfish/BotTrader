# -------------------------------
# Core runtime
# -------------------------------
ENV=aws
TZ=America/Anchorage
LOG_LEVEL=INFO
IS_DOCKER=true
IN_DOCKER=1
BOT_RUN_MODE=both          # webhook | sighook | both

# -------------------------------
# Database (local Postgres container)
# -------------------------------
DB_HOST=db
DB_PORT=5432
DB_NAME=bot_trader_db
DB_USER=bottrader
DB_PASSWORD=7317botTrade4ssm
DATABASE_URL=postgresql+asyncpg://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# -------------------------------
# Email / Reports (SMTP)
# Start with Gmail App Password or switch to SES by flipping host/user/pass
# -------------------------------
EMAIL_PROVIDER=smtp
EMAIL_FROM="BotTrader Reports imtradebot@gmail.com"
EMAIL_TO=Dennfish@gmail.com

# --- Gmail (testing) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=imtradebot@gmail.com
SMTP_PASSWORD=duwgzuzvdpnbpwkh

# --- SES (production) ---
# SMTP_HOST=email-smtp.us-west-2.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=CHANGEME_SES_SMTP_USERNAME
# SMTP_PASSWORD=CHANGEME_SES_SMTP_PASSWORD

# Optional alerting flags (if your code uses them)
EMAIL_ALERTS=False
ALERT_EMAIL=
ALERT_PHONE=
ACCOUNT_PHONE=          # keep if your alert code reads it

# -------------------------------
# Network / IP allow-list (optional if used by your app)
# -------------------------------
COIN_WHITELIST=127.0.0.1,45.144.80.19

# -------------------------------
# Exchange auth (Coinbase Advanced Trade)
# -------------------------------
#COINBASE_API_KEY=CHANGEME
#COINBASE_API_SECRET=CHANGEME
#COINBASE_API_PASSPHRASE=CHANGEME

# -------------------------------
# Trading / Strategy knobs
# (Carry over what your desktop env uses)
# -------------------------------
QUOTE_CURRENCY=USD

# Ratios / Bands
BUY_RATIO=1.05
SELL_RATIO=0.95
BB_WINDOW=10
BB_STD=2
BB_LOWER_BAND=1.0
BB_UPPER_BAND=1.1

# MACD / RSI / ATR
MACD_FAST=12
MACD_SLOW=26
MACD_SIGNAL=9
RSI_WINDOW=14
ATR_WINDOW=14

# ROC windows / thresholds (examples inferred from your desktop file)
ROC_5MIN=3
ROC_BUY_24H=5
ROC_SELL_24H=2
MIN_SPREAD_PCT=0.30

# Trailing & risk
TRAILING_PERCENTAGE=0.02
TRAILING_STOP=0.02
TRAILING_LIMIT=0.02
STOP_LOSS=-0.0075
TAKE_PROFIT=0.025

# Sizing / thresholds (adjust if your bot expects these)
BUY_SIZE_USD=60
MIN_ORDER_VALUE_USD=25

# -------------------------------
# App ports / web UI (if any)
# -------------------------------
APP_PORT=5003